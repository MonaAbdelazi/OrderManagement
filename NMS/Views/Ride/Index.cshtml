
@using NMS.Core.Resources
@using NMS.Data
@using GridMvc.Html
@model IEnumerable<NMS.Data.Ride>

@{
    ViewBag.Title = CommonRes.Index;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@CommonRes.Index</h2>

<div style="text-align: right;">
     <form action="ride/create">
          <button type="submit" class="btn btn-default alignPreview">
               <span class="glyphicon glyphicon-new-window"></span>
               @CommonRes.Button_CreateNew
          </button>
     </form>
</div>




<br />
@Html.Grid(Model).Named("ordersGrid").Columns(columns =>
{
        //columns.Add(model => model.Ride_Id).Sortable(true).SetWidth(50).Titled(NMSRes.Ride_Id);
	
    	columns.Add(model => model.count).Sortable(true).SetWidth(50).Titled(NMSRes.Count);
	
    	columns.Add(model => model.Amount).Sortable(true).SetWidth(50).Titled(NMSRes.Amount);
	
    	columns.Add(model => model.Type).Sortable(true).SetWidth(50).Titled(NMSRes.Type);
	
    	columns.Add(model => model.Emp_ID).Sortable(true).SetWidth(50).Titled(NMSRes.Emp_ID);
	
    	columns.Add(model => model.Invoice_ID).Sortable(true).SetWidth(50).Titled(NMSRes.Invoice_ID);
	
    	columns.Add(model => model.SirkID).Sortable(true).SetWidth(50).Titled(NMSRes.InSirkNo);
	
    	columns.Add(model => model.date).Sortable(true).SetWidth(50).Titled(NMSRes.date);
	
    	columns.Add(model => model.Status).Sortable(true).SetWidth(50).Titled(NMSRes.Status);
	
    	columns.Add(model => model.EnterdBy).Sortable(true).SetWidth(50).Titled(NMSRes.Entered_By);
	
    	columns.Add(model => model.ApprovedBy).Sortable(true).SetWidth(50).Titled(NMSRes.Approved_By);
	
    	columns.Add(model => model.Warehouse_ID).Sortable(true).SetWidth(50).Titled(NMSRes.Warehouse_ID);
	
    	columns.Add(model => model.Employess.Name).Sortable(true).SetWidth(50).Titled(NMSRes.Driver);
	
     
	
    	columns.Add(model => model.WareHouse.Name).Sortable(true).SetWidth(50).Titled(NMSRes.Warehouse_ID);
	
          columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Edit, "Edit", new { id = o.Ride_Id })).SetWidth(50);
                columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Detail, "Details", new { id = o.Ride_Id })).SetWidth(50);
                columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Delete, "Delete", new { id = o.Ride_Id })).SetWidth(50);
    
}).WithPaging(10).Filterable().SetLanguage(CommonRes.Lang)


